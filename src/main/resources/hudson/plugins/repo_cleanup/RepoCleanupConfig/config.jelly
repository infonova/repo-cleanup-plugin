<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:section title="${%RepoCleanup}">
    <f:entry title="${%Path to master repository}" field="pathToMasterRepo">
        <f:textbox/>
    </f:entry>
    <f:entry title="${%User for cross host repo sync}" field="user">
      <f:textbox/>
    </f:entry>
  </f:section>
</j:jelly>
